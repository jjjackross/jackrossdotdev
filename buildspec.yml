version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 12
  pre_build:
    commands:
      - npm install
      - npm install -g @angular/cli
  build:
    commands:
      - cd public
      - npm run build